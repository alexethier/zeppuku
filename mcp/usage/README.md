# usage

MCP that returns centralized usage/help text for tools across MCP servers.

The source content lives in:

- `src/aethier_mcp_usage/usage.yaml`

Run:

```bash
./bin/manager start usage
./bin/mcp usage tools
```
